---
layout: base.liquid
---
<!-- Display a listing item  -->
<section>
  <h2>{{ title }}</h2>
  <h3>Stock <span class="stock">#{{ stock }}</span></h3>
  <h3>ISBN <span class="isbn">{{ isbn }}</span></h3>
  <img class="cover" src="/images/{{ cover }}" alt="{{ title }}" >
  {{ content }}
</section>

